# PolarProcess Contracts

| Schema | Description |
|--------|-------------|
| `process-api.schema.json` | Service lifecycle + process management endpoints (`/api/services/*`, `/api/processes/*`) |
| `scheduler-api.schema.json` | Heavy task queue + scheduler endpoints (`/api/tasks/*`, `/api/scheduler/*`) |
